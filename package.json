{
  "name": "lessor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "predeploy": "npm run build && next export && node -e \"const fs=require('fs'); fs.writeFileSync('out/.nojekyll', '')\"",
    "deploy": "gh-pages -d out -t true",
    "start": "node index.js"
  },
  "dependencies": {
    "animejs": "^4.3.6",
    "next": "^13.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}